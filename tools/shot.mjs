// Screenshot harness — implemented in Prompt 2.
// Drives the dev server with Playwright, positions the camera at deterministic
// viewpoints and writes PNGs into shots/ for visual verification (see
// CLAUDE.md "Workflow").
